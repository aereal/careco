# yaml-language-server: $schema=./tests.schema.json
---
cases:
  'query totalStatistics/ok':
    request:
      query: |
        {
          totalStatistics {
            distanceKilometers
          }
        }
    response:
      data:
        totalStatistics:
          distanceKilometers: 45678
